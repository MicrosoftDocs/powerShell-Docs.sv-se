---
ms.date: 08/24/2018
keywords: PowerShell, cmdlet
title: Kommando namn för Learning PowerShell
ms.openlocfilehash: a65ffcdca6510093b0a77234e20546b6cc1f02bf
ms.sourcegitcommit: 6545c60578f7745be015111052fd7769f8289296
ms.translationtype: MT
ms.contentlocale: sv-SE
ms.lasthandoff: 04/22/2020
ms.locfileid: "67030427"
---
# <a name="learning-powershell-command-names"></a><span data-ttu-id="d875a-103">Kommando namn för Learning PowerShell</span><span class="sxs-lookup"><span data-stu-id="d875a-103">Learning PowerShell command names</span></span>

<span data-ttu-id="d875a-104">Inlärnings namn för kommandon och parametrar kräver en betydande tids investering med de flesta kommando rads gränssnitt.</span><span class="sxs-lookup"><span data-stu-id="d875a-104">Learning names of commands and parameters requires a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="d875a-105">Problemet är att det finns några mönster.</span><span class="sxs-lookup"><span data-stu-id="d875a-105">The issue is that there are few patterns.</span></span> <span data-ttu-id="d875a-106">Detta är det enda sättet att lära sig de kommandon och parametrar som du behöver använda regelbundet.</span><span class="sxs-lookup"><span data-stu-id="d875a-106">Memorization is the only way to learn the commands and parameters that you need to use on a regular basis.</span></span>

<span data-ttu-id="d875a-107">När du arbetar med ett nytt kommando eller parameter kan du inte alltid använda det du redan känner till.</span><span class="sxs-lookup"><span data-stu-id="d875a-107">When you work with a new command or parameter, you can't always use what you already know.</span></span> <span data-ttu-id="d875a-108">Du måste hitta och lära dig ett nytt namn.</span><span class="sxs-lookup"><span data-stu-id="d875a-108">You have to find and learn a new name.</span></span> <span data-ttu-id="d875a-109">Traditionellt är kommando rads gränssnitt som börjar med en liten uppsättning verktyg och växer med stegvisa tillägg.</span><span class="sxs-lookup"><span data-stu-id="d875a-109">Traditionally, command-line interfaces start with a small set of tools and grow with incremental additions.</span></span> <span data-ttu-id="d875a-110">Det är enkelt att se varför det inte finns någon standard struktur.</span><span class="sxs-lookup"><span data-stu-id="d875a-110">It's easy to see why there's no standard structure.</span></span>
<span data-ttu-id="d875a-111">Detta verkar logiskt för kommando namn eftersom varje kommando är ett separat verktyg.</span><span class="sxs-lookup"><span data-stu-id="d875a-111">This seems logical for command names since each command is a separate tool.</span></span> <span data-ttu-id="d875a-112">PowerShell har ett bättre sätt att hantera kommando namn.</span><span class="sxs-lookup"><span data-stu-id="d875a-112">PowerShell has a better way to handle command names.</span></span>

## <a name="learning-command-names-in-traditional-shells"></a><span data-ttu-id="d875a-113">Inlärnings kommando namn i traditionella gränssnitt</span><span class="sxs-lookup"><span data-stu-id="d875a-113">Learning command names in traditional shells</span></span>

<span data-ttu-id="d875a-114">De flesta kommandon är utformade för att hantera element i operativ systemet eller program, t. ex. tjänster eller processer.</span><span class="sxs-lookup"><span data-stu-id="d875a-114">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="d875a-115">Kommandona har namn som kanske inte passar in i en familj.</span><span class="sxs-lookup"><span data-stu-id="d875a-115">The commands have names that may or may not fit into a family.</span></span> <span data-ttu-id="d875a-116">I Windows-system kan du till exempel använda kommandona `net start` och `net stop` för att starta och stoppa en tjänst.</span><span class="sxs-lookup"><span data-stu-id="d875a-116">For example, on Windows systems, you can use the `net start` and `net stop` commands to start and stop a service.</span></span> <span data-ttu-id="d875a-117">**SC. exe** är ett annat tjänst kontroll verktyg för Windows.</span><span class="sxs-lookup"><span data-stu-id="d875a-117">**Sc.exe** is another service control tool for Windows.</span></span> <span data-ttu-id="d875a-118">Namnet passar inte in i namngivnings mönstret för **net. exe** -tjänstens kommandon.</span><span class="sxs-lookup"><span data-stu-id="d875a-118">That name does not fit into the naming pattern for the **net.exe** service commands.</span></span> <span data-ttu-id="d875a-119">För process hantering har Windows kommandot **Tasklist. exe** för att visa en lista över processer och kommandot **taskkill. exe** för att avsluta processer.</span><span class="sxs-lookup"><span data-stu-id="d875a-119">For process management, Windows has the **tasklist.exe** command to list processes and the **taskkill.exe** command to kill processes.</span></span>

<span data-ttu-id="d875a-120">Dessa kommandon har också oregelbundna parameter specifikationer.</span><span class="sxs-lookup"><span data-stu-id="d875a-120">Also, these commands have irregular parameter specifications.</span></span> <span data-ttu-id="d875a-121">Du kan inte använda `net start` kommandot för att starta en tjänst på en fjärrdator.</span><span class="sxs-lookup"><span data-stu-id="d875a-121">You can't use the `net start` command to start a service on a remote computer.</span></span> <span data-ttu-id="d875a-122">Kommandot **SC. exe** kan starta en tjänst på en fjärrdator.</span><span class="sxs-lookup"><span data-stu-id="d875a-122">The **sc.exe** command can start a service on a remote computer.</span></span> <span data-ttu-id="d875a-123">Men för att ange fjärrdatorn måste du ange ett prefix för namnet med ett dubbelt omvänt snedstreck.</span><span class="sxs-lookup"><span data-stu-id="d875a-123">But to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="d875a-124">Om du vill starta Spooler-tjänsten på en fjärrdator med namnet DC01 skriver `sc.exe \\DC01 start spooler`du.</span><span class="sxs-lookup"><span data-stu-id="d875a-124">To start the spooler service on a remote computer named DC01, you type `sc.exe \\DC01 start spooler`.</span></span>
<span data-ttu-id="d875a-125">Om du vill visa en lista över aktiviteter som körs på DC01 använder du parametern **/s** och dator namnet utan omvänt snedstreck.</span><span class="sxs-lookup"><span data-stu-id="d875a-125">To list tasks running on DC01, you use the **/S** parameter and the computer name without backslashes.</span></span> <span data-ttu-id="d875a-126">Till exempel `tasklist /S DC01`.</span><span class="sxs-lookup"><span data-stu-id="d875a-126">For example, `tasklist /S DC01`.</span></span>

> [!NOTE]
> <span data-ttu-id="d875a-127">Innan PowerShell V6 `sc` var ett alias för `Set-Content` cmdleten.</span><span class="sxs-lookup"><span data-stu-id="d875a-127">Prior to PowerShell v6, `sc` was an alias for the `Set-Content` cmdlet.</span></span> <span data-ttu-id="d875a-128">För att köra kommandot **SC. exe** i en version av PowerShell före V6 måste du därför inkludera det fullständiga fil namnet **SC. exe** , inklusive fil namns tillägget **exe**.</span><span class="sxs-lookup"><span data-stu-id="d875a-128">Therefore, to run the **sc.exe** command in a version of PowerShell prior to v6, you must include the full filename **sc.exe** including the file extension **exe**.</span></span>

<span data-ttu-id="d875a-129">Tjänster och processer är exempel på hanterbara element på en dator som har väldefinierade livs cykel cyklar.</span><span class="sxs-lookup"><span data-stu-id="d875a-129">Services and processes are examples of manageable elements on a computer that have well-defined life cycles.</span></span> <span data-ttu-id="d875a-130">Du kan starta eller stoppa tjänster och processer eller hämta en lista över alla tjänster eller processer som körs.</span><span class="sxs-lookup"><span data-stu-id="d875a-130">You may start or stop services and processes, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="d875a-131">Även om det finns viktiga tekniska skillnader mellan dem, är de åtgärder som du utför på tjänster och processer konceptuellt desamma.</span><span class="sxs-lookup"><span data-stu-id="d875a-131">Although there are important technical distinctions between them, the actions you perform on services and processes are conceptually the same.</span></span> <span data-ttu-id="d875a-132">Dessutom kan de val vi gör för att anpassa en åtgärd genom att ange parametrar vara konceptuellt lika bra.</span><span class="sxs-lookup"><span data-stu-id="d875a-132">Furthermore, the choices we make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="d875a-133">PowerShell utnyttjar dessa likheter för att minska antalet distinkta namn som du behöver känna till för att förstå och använda cmdlets.</span><span class="sxs-lookup"><span data-stu-id="d875a-133">PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

## <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="d875a-134">Cmdlet: ar använder verb-Substantiv-namn för att minimera kommandots-subst</span><span class="sxs-lookup"><span data-stu-id="d875a-134">Cmdlets use verb-noun names to reduce command memorization</span></span>

<span data-ttu-id="d875a-135">PowerShell använder namngivnings systemet "verb-Substantiv".</span><span class="sxs-lookup"><span data-stu-id="d875a-135">PowerShell uses a "verb-noun" naming system.</span></span> <span data-ttu-id="d875a-136">Varje cmdlet-namn består av ett standard-verb avstavat med ett bestämt substantiv.</span><span class="sxs-lookup"><span data-stu-id="d875a-136">Each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="d875a-137">PowerShell-verb är inte alltid engelska verb, utan de uttrycker vissa åtgärder i PowerShell.</span><span class="sxs-lookup"><span data-stu-id="d875a-137">PowerShell verbs are not always English verbs, but they express specific actions in PowerShell.</span></span> <span data-ttu-id="d875a-138">Substantiv är mycket likt Substantiv på valfritt språk.</span><span class="sxs-lookup"><span data-stu-id="d875a-138">Nouns are very much like nouns in any language.</span></span> <span data-ttu-id="d875a-139">De beskriver vissa typer av objekt som är viktiga för system administration.</span><span class="sxs-lookup"><span data-stu-id="d875a-139">They describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="d875a-140">Det är enkelt att visa hur dessa namn på två delar minskar inlärnings ansträngningen genom att titta på några exempel.</span><span class="sxs-lookup"><span data-stu-id="d875a-140">It's easy to demonstrate how these two-part names reduce learning effort by looking at a few examples.</span></span>

<span data-ttu-id="d875a-141">PowerShell har en rekommenderad uppsättning standard-verb.</span><span class="sxs-lookup"><span data-stu-id="d875a-141">PowerShell has a recommended set of standard verbs.</span></span> <span data-ttu-id="d875a-142">Substantiv är mindre begränsade, men beskriver alltid vad verbet agerar på.</span><span class="sxs-lookup"><span data-stu-id="d875a-142">Nouns are less restricted, but always describe what the verb acts upon.</span></span> <span data-ttu-id="d875a-143">PowerShell har kommandon `Get-Process`som, `Stop-Process` `Get-Service`, och. `Stop-Service`</span><span class="sxs-lookup"><span data-stu-id="d875a-143">PowerShell has commands such as `Get-Process`, `Stop-Process`, `Get-Service`, and `Stop-Service`.</span></span>

<span data-ttu-id="d875a-144">I det här exemplet på två Substantiv och verb fören klar konsekvensen inte inlärningen mycket.</span><span class="sxs-lookup"><span data-stu-id="d875a-144">For this example of two nouns and verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="d875a-145">Utöka listan till en standardiserad uppsättning med 10 verb och 10 substantiv.</span><span class="sxs-lookup"><span data-stu-id="d875a-145">Extend that list to a standardized set of 10 verbs and 10 nouns.</span></span> <span data-ttu-id="d875a-146">Nu har du bara 20 ord att förstå.</span><span class="sxs-lookup"><span data-stu-id="d875a-146">Now you only have 20 words to understand.</span></span>
<span data-ttu-id="d875a-147">Men dessa ord kan kombineras med formatet 100 distinkta kommando namn.</span><span class="sxs-lookup"><span data-stu-id="d875a-147">But those words can be combined to form 100 distinct command names.</span></span>

<span data-ttu-id="d875a-148">Det är enkelt att förstå vad ett PowerShell-kommando gör genom att läsa dess namn.</span><span class="sxs-lookup"><span data-stu-id="d875a-148">It's easy to understand what a PowerShell command does by reading its name.</span></span> <span data-ttu-id="d875a-149">Kommandot för att stänga av en dator är `Stop-Computer`.</span><span class="sxs-lookup"><span data-stu-id="d875a-149">The command to shut down a computer is `Stop-Computer`.</span></span> <span data-ttu-id="d875a-150">Kommandot för att visa en lista över alla datorer i `Get-Computer`ett nätverk är.</span><span class="sxs-lookup"><span data-stu-id="d875a-150">The command to list all computers on a network is `Get-Computer`.</span></span> <span data-ttu-id="d875a-151">Kommandot för att hämta system datumet är `Get-Date`.</span><span class="sxs-lookup"><span data-stu-id="d875a-151">The command to get the system date is `Get-Date`.</span></span>

<span data-ttu-id="d875a-152">Du kan visa alla kommandon som innehåller ett visst verb med parametern **verb** för `Get-Command`.</span><span class="sxs-lookup"><span data-stu-id="d875a-152">You can list all commands that include a particular verb with the **Verb** parameter for `Get-Command`.</span></span> <span data-ttu-id="d875a-153">Om du till exempel vill visa alla cmdletar som använder verbet `Get`skriver du:</span><span class="sxs-lookup"><span data-stu-id="d875a-153">For example, to see all cmdlets that use the verb `Get`, type:</span></span>

```
PS> Get-Command -Verb Get

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="d875a-154">Använd parametern **Substantiv** för att se en serie kommandon som påverkar samma typ av objekt.</span><span class="sxs-lookup"><span data-stu-id="d875a-154">Use the **Noun** parameter to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="d875a-155">Kör till exempel följande kommando för att se vilka kommandon som är tillgängliga för hantering av tjänster:</span><span class="sxs-lookup"><span data-stu-id="d875a-155">For example, run following command to see the commands  available for managing services:</span></span>

```
PS> Get-Command -Noun Service

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

## <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="d875a-156">Cmdlets använder standard parametrar</span><span class="sxs-lookup"><span data-stu-id="d875a-156">Cmdlets use standard parameters</span></span>

<span data-ttu-id="d875a-157">Som tidigare nämnts har kommandon som används i traditionella kommando rads gränssnitt inte alltid konsekventa parameter namn.</span><span class="sxs-lookup"><span data-stu-id="d875a-157">As noted earlier, commands used in traditional command-line interfaces don't always have consistent parameter names.</span></span> <span data-ttu-id="d875a-158">Parametrar är ofta enkla eller förkortade ord som är lätta att skriva, men som inte är lätta att förstå för nya användare.</span><span class="sxs-lookup"><span data-stu-id="d875a-158">Parameters are often single-character or abbreviated words that are easy to type but aren't easily understood by new users.</span></span>

<span data-ttu-id="d875a-159">Till skillnad från de flesta andra traditionella kommando rads gränssnitt, bearbetar PowerShell parametrar direkt och den använder direkt åtkomst till parametrarna tillsammans med vägledning för utvecklare för att standardisera parameter namn.</span><span class="sxs-lookup"><span data-stu-id="d875a-159">Unlike most other traditional command-line interfaces, PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="d875a-160">Den här vägledningen uppmuntrar men garanterar inte att varje cmdlet följer standarden.</span><span class="sxs-lookup"><span data-stu-id="d875a-160">This guidance encourages but does not guarantee that every cmdlet conforms to the standard.</span></span>

<span data-ttu-id="d875a-161">PowerShell standardiserar också parameter avgränsaren.</span><span class="sxs-lookup"><span data-stu-id="d875a-161">PowerShell also standardizes the parameter separator.</span></span> <span data-ttu-id="d875a-162">Parameter namn har alltid en-anpassningsprefix till dem med ett PowerShell-kommando.</span><span class="sxs-lookup"><span data-stu-id="d875a-162">Parameter names always have a '-' prepended to them with a PowerShell command.</span></span> <span data-ttu-id="d875a-163">Ta följande som exempel:</span><span class="sxs-lookup"><span data-stu-id="d875a-163">Consider the following example:</span></span>

```powershell
Get-Command -Name Clear-Host
```

<span data-ttu-id="d875a-164">Parameterns namn är **ett namn**, men den skrivs som `-Name` när den används på kommando raden som en parameter.</span><span class="sxs-lookup"><span data-stu-id="d875a-164">The parameter's name is **Name**, but it is typed as `-Name` when used on the command line as a parameter.</span></span>

<span data-ttu-id="d875a-165">Här följer några allmänna egenskaper för standard parameter namn och-användning.</span><span class="sxs-lookup"><span data-stu-id="d875a-165">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

### <a name="the-help-parameter-"></a><span data-ttu-id="d875a-166">Parametern Help (?)</span><span class="sxs-lookup"><span data-stu-id="d875a-166">The Help parameter (?)</span></span>

<span data-ttu-id="d875a-167">När du anger `-?` parametern för alla cmdletar visar PowerShell hjälp för cmdleten.</span><span class="sxs-lookup"><span data-stu-id="d875a-167">When you specify the `-?` parameter on any cmdlet, PowerShell displays help for the cmdlet.</span></span>
<span data-ttu-id="d875a-168">Cmdleten körs inte.</span><span class="sxs-lookup"><span data-stu-id="d875a-168">The cmdlet is not executed.</span></span>

### <a name="common-parameters"></a><span data-ttu-id="d875a-169">Vanliga parametrar</span><span class="sxs-lookup"><span data-stu-id="d875a-169">Common parameters</span></span>

<span data-ttu-id="d875a-170">PowerShell har flera *vanliga parametrar*.</span><span class="sxs-lookup"><span data-stu-id="d875a-170">PowerShell has several *common parameters*.</span></span> <span data-ttu-id="d875a-171">Dessa parametrar styrs av PowerShell-motorn.</span><span class="sxs-lookup"><span data-stu-id="d875a-171">These parameters are controlled by the PowerShell engine.</span></span> <span data-ttu-id="d875a-172">Vanliga parametrar fungerar alltid på samma sätt.</span><span class="sxs-lookup"><span data-stu-id="d875a-172">Common parameters always behave the same way.</span></span> <span data-ttu-id="d875a-173">De gemensamma parametrarna är **whatIf**, **Confirm**, **verbose**, **Debug**, **Warning**, **ErrorAction**, **ErrorVariable**, **devariable**och **utbuffer**.</span><span class="sxs-lookup"><span data-stu-id="d875a-173">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

### <a name="recommended-parameter-names"></a><span data-ttu-id="d875a-174">Rekommenderade parameter namn</span><span class="sxs-lookup"><span data-stu-id="d875a-174">Recommended parameter names</span></span>

<span data-ttu-id="d875a-175">PowerShell Core-cmdletar använder standard namn för liknande parametrar.</span><span class="sxs-lookup"><span data-stu-id="d875a-175">The PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="d875a-176">Användningen av dessa standard namn är inte tvingande, men det finns uttryckliga rikt linjer för att uppmuntra standardisering.</span><span class="sxs-lookup"><span data-stu-id="d875a-176">The use of these standard names is not enforced, but there is explicit guidance to encourage standardization.</span></span>

<span data-ttu-id="d875a-177">Till exempel är det rekommenderade namnet för en parameter som refererar till en dator **computername**, i stället för Server, värd, system, nod eller något annat vanligt alternativ.</span><span class="sxs-lookup"><span data-stu-id="d875a-177">For example, the recommended name for a parameter that refers to a computer is **ComputerName**, rather than Server, Host, System, Node, or some other common alternative.</span></span> <span data-ttu-id="d875a-178">Andra viktiga rekommenderade parameter namn är **Force**, **exclude**, **include**, **Passthru**, **Path**och **caseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="d875a-178">Other important recommended parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>
