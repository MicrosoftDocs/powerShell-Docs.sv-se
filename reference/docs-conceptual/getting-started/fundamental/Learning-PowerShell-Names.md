---
ms.date: 08/24/2018
keywords: PowerShell cmdlet
title: Lär dig PowerShell-namn
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 6ed1f99513f00c174147876e1982c0d12869cacb
ms.sourcegitcommit: 221b7daab7f597f8b2e4864cf9b5d9dda9b9879b
ms.translationtype: MT
ms.contentlocale: sv-SE
ms.lasthandoff: 11/27/2018
ms.locfileid: "52320490"
---
# <a name="learning-powershell-names"></a><span data-ttu-id="1ef7e-103">Lär dig PowerShell-namn</span><span class="sxs-lookup"><span data-stu-id="1ef7e-103">Learning PowerShell names</span></span>

<span data-ttu-id="1ef7e-104">Learning namnen på kommandon och parametrar kräver en betydande tid investering med de flesta command-line gränssnitt.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-104">Learning names of commands and parameters requires a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="1ef7e-105">Problemet är att det inte finns några mönster.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-105">The issue is that there are few patterns.</span></span> <span data-ttu-id="1ef7e-106">Memorera är det enda sättet att lära dig kommandon och parametrar som du behöver använda regelbundet.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-106">Memorization is the only way to learn the commands and parameters that you need to use on a regular basis.</span></span>

<span data-ttu-id="1ef7e-107">När du arbetar med ett nytt kommando eller en parameter, det går inte att du alltid använda det du redan kan.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-107">When you work with a new command or parameter, you can't always use what you already know.</span></span> <span data-ttu-id="1ef7e-108">Du måste ta och lär dig ett nytt namn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-108">You have to find and learn a new name.</span></span> <span data-ttu-id="1ef7e-109">Traditionellt command-line gränssnitt börja med en liten uppsättning verktyg och växa med inkrementell tillägg.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-109">Traditionally, command-line interfaces start with a small set of tools and grow with incremental additions.</span></span> <span data-ttu-id="1ef7e-110">Det är enkelt att se varför det är ingen standard struktur.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-110">It's easy to see why there's no standard structure.</span></span>
<span data-ttu-id="1ef7e-111">Det ser ut logiska efter kommandonamn eftersom varje kommando är ett separat verktyg.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-111">This seems logical for command names since each command is a separate tool.</span></span> <span data-ttu-id="1ef7e-112">PowerShell har ett bättre sätt att hantera kommandonamn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-112">PowerShell has a better way to handle command names.</span></span>

## <a name="learning-command-names-in-traditional-shells"></a><span data-ttu-id="1ef7e-113">Learning kommandonamn i traditionella gränssnitt</span><span class="sxs-lookup"><span data-stu-id="1ef7e-113">Learning command names in traditional shells</span></span>

<span data-ttu-id="1ef7e-114">De flesta kommandon är utformat för att hantera element av operativsystem eller program, till exempel tjänster eller processer.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-114">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="1ef7e-115">Kommandon har namn som kanske eller kanske inte passar i en serie.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-115">The commands have names that may or may not fit into a family.</span></span> <span data-ttu-id="1ef7e-116">Till exempel på Windows-System, du kan använda den `net start` och `net stop` kommandon för att starta och stoppa en tjänst.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-116">For example, on Windows systems, you can use the `net start` and `net stop` commands to start and stop a service.</span></span> <span data-ttu-id="1ef7e-117">**SC.exe** är ett annat service control-verktyg för Windows.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-117">**Sc.exe** is another service control tool for Windows.</span></span> <span data-ttu-id="1ef7e-118">Det namnet inte passar i namngivningsmönstret för den **net.exe** tjänsten kommandon.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-118">That name does not fit into the naming pattern for the **net.exe** service commands.</span></span> <span data-ttu-id="1ef7e-119">För hantering av affärsprocesser, Windows har det **tasklist.exe** för att lista processer och **taskkill.exe** kommando för att avsluta processer.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-119">For process management, Windows has the **tasklist.exe** command to list processes and the **taskkill.exe** command to kill processes.</span></span>

<span data-ttu-id="1ef7e-120">De här kommandona kräver dessutom oregelbunden parametern specifikationer.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-120">Also, these commands have irregular parameter specifications.</span></span> <span data-ttu-id="1ef7e-121">Du kan inte använda den `net start` kommando för att starta en tjänst på en fjärrdator.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-121">You can't use the `net start` command to start a service on a remote computer.</span></span> <span data-ttu-id="1ef7e-122">Den **sc.exe** kommando kan starta en tjänst på en fjärrdator.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-122">The **sc.exe** command can start a service on a remote computer.</span></span> <span data-ttu-id="1ef7e-123">Men om du vill ange fjärrdatorn måste du ange prefixet dess namn med två omvända snedstreck.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-123">But to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="1ef7e-124">Om du vill starta tjänsten spooler på en fjärrdator med namnet DC01, skriver du `sc.exe \\DC01 start spooler`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-124">To start the spooler service on a remote computer named DC01, you type `sc.exe \\DC01 start spooler`.</span></span>
<span data-ttu-id="1ef7e-125">Listan aktiviteter som körs på DC01, som du använder den **/S** parametern och namnet på datorn utan omvända snedstreck.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-125">To list tasks running on DC01, you use the **/S** parameter and the computer name without backslashes.</span></span> <span data-ttu-id="1ef7e-126">Till exempel `tasklist /S DC01`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-126">For example, `tasklist /S DC01`.</span></span>

> [!NOTE]
> <span data-ttu-id="1ef7e-127">Innan du PowerShell v6 `sc` har ett alias för den `Set-Content` cmdlet.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-127">Prior to PowerShell v6, `sc` was an alias for the `Set-Content` cmdlet.</span></span> <span data-ttu-id="1ef7e-128">Att köra den **sc.exe** kommandot, måste du inkludera filnamnstillägget.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-128">To run the **sc.exe** command, you must include the file extension.</span></span>

<span data-ttu-id="1ef7e-129">Tjänster och processer är exempel på hanterbara element på en dator som har väldefinierade supportlivscykel.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-129">Services and processes are examples of manageable elements on a computer that have well-defined life cycles.</span></span> <span data-ttu-id="1ef7e-130">Du kan starta eller stoppa tjänster och processer eller hämta en lista över alla tjänster eller processer som körs.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-130">You may start or stop services and processes, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="1ef7e-131">Även om det finns viktiga tekniska skillnader mellan dem, är de åtgärder du kan utföra på tjänster och processer begreppsmässigt samma.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-131">Although there are important technical distinctions between them, the actions you perform on services and processes are conceptually the same.</span></span> <span data-ttu-id="1ef7e-132">Dessutom är kan de val som vi gör att anpassa en åtgärd genom att ange parametrar vara begreppsmässigt liknas samt.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-132">Furthermore, the choices we make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="1ef7e-133">PowerShell utnyttjar dessa likheter för att minska antalet distinkta namn som du behöver veta för att förstå och använda cmdlets.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-133">PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

## <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="1ef7e-134">Cmdlet: ar använda verb-substantiv namn för att minska kommandot memorera</span><span class="sxs-lookup"><span data-stu-id="1ef7e-134">Cmdlets use verb-noun names to reduce command memorization</span></span>

<span data-ttu-id="1ef7e-135">PowerShell använder ett namngivning system för ”verb-substantiv”.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-135">PowerShell uses a "verb-noun" naming system.</span></span> <span data-ttu-id="1ef7e-136">Varje cmdlet-namn som består av ett standard verb som ska ha bindestreck med en specifik substantiv.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-136">Each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="1ef7e-137">PowerShell-verb är inte alltid engelska verb, men de express specifika åtgärder i PowerShell.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-137">PowerShell verbs are not always English verbs, but they express specific actions in PowerShell.</span></span> <span data-ttu-id="1ef7e-138">Substantiv är ungefär samma sätt som substantiv på valfritt språk.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-138">Nouns are very much like nouns in any language.</span></span> <span data-ttu-id="1ef7e-139">De beskriver vissa typer av objekt som är viktiga för systemadministration.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-139">They describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="1ef7e-140">Det är enkelt att demonstrera hur dessa två delnamn minska learning arbete genom att titta på några exempel.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-140">It's easy to demonstrate how these two-part names reduce learning effort by looking at a few examples.</span></span>

<span data-ttu-id="1ef7e-141">PowerShell har en rekommenderad uppsättning standardverb.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-141">PowerShell has a recommended set of standard verbs.</span></span> <span data-ttu-id="1ef7e-142">Substantiv är mindre begränsade men alltid beskriver verb agerar på.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-142">Nouns are less restricted, but always describe what the verb acts upon.</span></span> <span data-ttu-id="1ef7e-143">PowerShell har kommandon som `Get-Process`, `Stop-Process`, `Get-Service`, och `Stop-Service`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-143">PowerShell has commands such as `Get-Process`, `Stop-Process`, `Get-Service`, and `Stop-Service`.</span></span>

<span data-ttu-id="1ef7e-144">I det här exemplet på två substantiv och verb förenkla konsekvens inte lära dig mycket.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-144">For this example of two nouns and verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="1ef7e-145">Utöka listan för att en uppsättning 10 verb och substantiv 10.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-145">Extend that list to a standardized set of 10 verbs and 10 nouns.</span></span> <span data-ttu-id="1ef7e-146">Nu har du bara 20 ord att förstå.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-146">Now you only have 20 words to understand.</span></span>
<span data-ttu-id="1ef7e-147">Men orden kan kombineras för att skapa 100 distinkta kommandonamn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-147">But those words can be combined to form 100 distinct command names.</span></span>

<span data-ttu-id="1ef7e-148">Det är lätt att förstå vad ett PowerShell-kommando gör genom att läsa dess namn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-148">It's easy to understand what a PowerShell command does by reading its name.</span></span> <span data-ttu-id="1ef7e-149">Kommandot för att stänga av datorn är `Stop-Computer`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-149">The command to shut down a computer is `Stop-Computer`.</span></span> <span data-ttu-id="1ef7e-150">Kommando för att lista alla datorer i ett nätverk är `Get-Computer`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-150">The command to list all computers on a network is `Get-Computer`.</span></span> <span data-ttu-id="1ef7e-151">Kommando för att hämta systemdatumet är `Get-Date`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-151">The command to get the system date is `Get-Date`.</span></span>

<span data-ttu-id="1ef7e-152">Du kan lista alla kommandon som innehåller ett visst verb med den **Verb** parametern för `Get-Command`.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-152">You can list all commands that include a particular verb with the **Verb** parameter for `Get-Command`.</span></span> <span data-ttu-id="1ef7e-153">Till exempel vill se alla cmdletar med verbet `Get`, typ:</span><span class="sxs-lookup"><span data-stu-id="1ef7e-153">For example, to see all cmdlets that use the verb `Get`, type:</span></span>

```
PS> Get-Command -Verb Get

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="1ef7e-154">Använd den **substantiv** parametern för att se en familj av kommandon som påverkar samma typ av objekt.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-154">Use the **Noun** parameter to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="1ef7e-155">Till exempel köra följande kommando för att se kommandona som är tillgängliga för att hantera tjänster:</span><span class="sxs-lookup"><span data-stu-id="1ef7e-155">For example, run following command to see the commands  available for managing services:</span></span>

```
PS> Get-Command -Noun Service

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

## <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="1ef7e-156">Cmdletar använder standardparametrar</span><span class="sxs-lookup"><span data-stu-id="1ef7e-156">Cmdlets use standard parameters</span></span>

<span data-ttu-id="1ef7e-157">Som tidigare nämnts har kommandon som används i traditionella command-line gränssnitt inte alltid konsekventa parameternamn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-157">As noted earlier, commands used in traditional command-line interfaces don't always have consistent parameter names.</span></span> <span data-ttu-id="1ef7e-158">Parametrarna är ofta tecken eller förkortas ord som är lätta att skriva men som inte är lätt att förstå genom att nya användare.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-158">Parameters are often single-character or abbreviated words that are easy to type but aren't easily understood by new users.</span></span>

<span data-ttu-id="1ef7e-159">Till skillnad från de flesta andra traditionella command-line gränssnitt PowerShell bearbetar parametrar direkt och den använder den här direkt åtkomst till parametrarna tillsammans med vägledning för utvecklare för att standardisera parameternamn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-159">Unlike most other traditional command-line interfaces, PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="1ef7e-160">Den här vägledningen uppmuntrar men garanterar inte att alla cmdlets som följer standard.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-160">This guidance encourages but does not guarantee that every cmdlet conforms to the standard.</span></span>

<span data-ttu-id="1ef7e-161">PowerShell standardiserar också parametern avgränsare.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-161">PowerShell also standardizes the parameter separator.</span></span> <span data-ttu-id="1ef7e-162">Parameternamn har alltid en '-' tillagt för dem med ett PowerShell-kommando.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-162">Parameter names always have a '-' prepended to them with a PowerShell command.</span></span> <span data-ttu-id="1ef7e-163">Överväg följande exempel:</span><span class="sxs-lookup"><span data-stu-id="1ef7e-163">Consider the following example:</span></span>

```powershell
Get-Command -Name Clear-Host
```

<span data-ttu-id="1ef7e-164">Parameternamnet är **namn**, men det skrivs som `-Name` när de används på kommandoraden som en parameter.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-164">The parameter's name is **Name**, but it is typed as `-Name` when used on the command line as a parameter.</span></span>

<span data-ttu-id="1ef7e-165">Här är några av de allmänna egenskaperna hos standard parameternamn och användningsområden.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-165">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

### <a name="the-help-parameter-"></a><span data-ttu-id="1ef7e-166">Hjälp-parametern (?)</span><span class="sxs-lookup"><span data-stu-id="1ef7e-166">The Help parameter (?)</span></span>

<span data-ttu-id="1ef7e-167">När du anger den `-?` parametern på alla cmdletar PowerShell visar hjälpen för cmdleten.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-167">When you specify the `-?` parameter on any cmdlet, PowerShell displays help for the cmdlet.</span></span>
<span data-ttu-id="1ef7e-168">Cmdleten körs inte.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-168">The cmdlet is not executed.</span></span>

### <a name="common-parameters"></a><span data-ttu-id="1ef7e-169">Gemensamma parametrar</span><span class="sxs-lookup"><span data-stu-id="1ef7e-169">Common parameters</span></span>

<span data-ttu-id="1ef7e-170">PowerShell har flera *gemensamma parametrar*.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-170">PowerShell has several *common parameters*.</span></span> <span data-ttu-id="1ef7e-171">Dessa parametrar styrs av PowerShell-motorn.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-171">These parameters are controlled by the PowerShell engine.</span></span> <span data-ttu-id="1ef7e-172">Gemensamma parametrar fungerar alltid på samma sätt.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-172">Common parameters always behave the same way.</span></span> <span data-ttu-id="1ef7e-173">De gemensamma parametrarna är **WhatIf**, **Bekräfta**, **utförlig**, **felsöka**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, och **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-173">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

### <a name="recommended-parameter-names"></a><span data-ttu-id="1ef7e-174">Rekommenderade parameternamn</span><span class="sxs-lookup"><span data-stu-id="1ef7e-174">Recommended parameter names</span></span>

<span data-ttu-id="1ef7e-175">PowerShell core-cmdletarna använda standardnamn för liknande parametrar.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-175">The PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="1ef7e-176">Användningen av dessa standardnamn tillämpas inte, men det finns explicita vägledning för att uppmuntra standardisering.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-176">The use of these standard names is not enforced, but there is explicit guidance to encourage standardization.</span></span>

<span data-ttu-id="1ef7e-177">Till exempel rekommenderade namnet för en parameter som refererar till en dator är **ComputerName**, i stället för Server, värden, System, noden eller andra vanliga alternativ.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-177">For example, the recommended name for a parameter that refers to a computer is **ComputerName**, rather than Server, Host, System, Node, or some other common alternative.</span></span> <span data-ttu-id="1ef7e-178">Andra viktiga rekommenderade parameternamn är **kraft**, **undanta**, **inkludera**, **PassThru**, **sökväg**, och **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="1ef7e-178">Other important recommended parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>
