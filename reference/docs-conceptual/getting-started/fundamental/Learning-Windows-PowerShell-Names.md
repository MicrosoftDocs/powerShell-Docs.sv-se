---
ms.date: 06/05/2017
keywords: PowerShell-cmdlet
title: Läs mer om Windows PowerShell-namn
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 381aa619a41ccacb2ff3a4cdbc2b75b7f04282d1
ms.sourcegitcommit: cf195b090b3223fa4917206dfec7f0b603873cdf
ms.translationtype: MT
ms.contentlocale: sv-SE
ms.lasthandoff: 04/09/2018
---
# <a name="learning-windows-powershell-names"></a><span data-ttu-id="f2c03-103">Läs mer om Windows PowerShell-namn</span><span class="sxs-lookup"><span data-stu-id="f2c03-103">Learning Windows PowerShell Names</span></span>
<span data-ttu-id="f2c03-104">Learning namnen på kommandon och parametrar för kommandot är en betydande tid investering med de flesta kommandoradsverktyget gränssnitt.</span><span class="sxs-lookup"><span data-stu-id="f2c03-104">Learning names of commands and command parameters is a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="f2c03-105">Problemet är att det finns mycket få mönster, så det enda sättet att lära dig om du lär dig varje kommando och varje parameter som du behöver använda regelbundet.</span><span class="sxs-lookup"><span data-stu-id="f2c03-105">The issue is that there are very few patterns, so the only way to learn is by memorizing each command and each parameter that you need to use on a regular basis.</span></span>

<span data-ttu-id="f2c03-106">När du arbetar med ett nytt kommando eller en parameter använda du normalt inte vad du redan vet; Du måste ta och lär dig ett nytt namn.</span><span class="sxs-lookup"><span data-stu-id="f2c03-106">When you work with a new command or parameter, you cannot generally use what you already know; you have to find and learn a new name.</span></span> <span data-ttu-id="f2c03-107">Om du tittar på hur gränssnitt växa från en liten uppsättning verktyg med inkrementell tillägg till funktioner är det enkelt att se varför strukturen är inte är standard.</span><span class="sxs-lookup"><span data-stu-id="f2c03-107">If you look at how interfaces grow from a small set of tools with incremental additions to functionality, it is easy to see why the structure is nonstandard.</span></span> <span data-ttu-id="f2c03-108">Med kommandonamn i synnerhet är det här kan låta logiska eftersom varje kommando är ett fristående verktyg, men det finns ett bättre sätt att hantera kommandonamn.</span><span class="sxs-lookup"><span data-stu-id="f2c03-108">With command names in particular, this may sound logical since each command is a separate tool, but there is a better way to handle command names.</span></span>

<span data-ttu-id="f2c03-109">De flesta kommandon skapas för att hantera element av operativsystem och program, till exempel tjänster eller processer.</span><span class="sxs-lookup"><span data-stu-id="f2c03-109">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="f2c03-110">Kommandona har olika namn som kanske eller kanske inte får plats i en familj.</span><span class="sxs-lookup"><span data-stu-id="f2c03-110">The commands have a variety of names that may or may not fit into a family.</span></span> <span data-ttu-id="f2c03-111">Till exempel på Windows-System kan du använda den **net start** och **net stop** kommandon för att starta och stoppa en tjänst.</span><span class="sxs-lookup"><span data-stu-id="f2c03-111">For example, on Windows systems, you can use the **net start** and **net stop** commands to start and stop a service.</span></span> <span data-ttu-id="f2c03-112">Det finns en annan mer generaliserade service control verktyg för Windows som har ett fullständigt namn, **sc**, som inte passar i namngivningsmönster för den **net** tjänsten kommandon.</span><span class="sxs-lookup"><span data-stu-id="f2c03-112">There is another more generalized service control tool for Windows that has a completely different name, **sc**, that does not fit into the naming pattern for the **net** service commands.</span></span> <span data-ttu-id="f2c03-113">För hanteringen, Windows har det **tasklist** kommando till listan processer och **taskkill** kommando för att avsluta processer.</span><span class="sxs-lookup"><span data-stu-id="f2c03-113">For process management, Windows has the **tasklist** command to list processes and the **taskkill** command to kill processes.</span></span>

<span data-ttu-id="f2c03-114">Kommandon som parametrar har oregelbundna parametern specifikationer.</span><span class="sxs-lookup"><span data-stu-id="f2c03-114">Commands that take parameters have irregular parameter specifications.</span></span> <span data-ttu-id="f2c03-115">Du kan inte använda den **net start** kommando för att starta en tjänst på en fjärrdator.</span><span class="sxs-lookup"><span data-stu-id="f2c03-115">You cannot use the **net start** command to start a service on a remote computer.</span></span> <span data-ttu-id="f2c03-116">Den **sc** kommando startar en tjänst på en fjärrdator, men om du vill ange fjärrdatorn måste du ange prefixet dess namn med ett dubbelt omvänt snedstreck.</span><span class="sxs-lookup"><span data-stu-id="f2c03-116">The **sc** command will start a service on a remote computer, but to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="f2c03-117">Till exempel om du vill starta utskriftshanteraren på en fjärrdator med namnet DC01, skriver du **sc \\ \\DC01 starta utskriftshanterarens**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-117">For example, to start the spooler service on a remote computer named DC01, you would type **sc \\\\DC01 start spooler**.</span></span> <span data-ttu-id="f2c03-118">Listan aktiviteter som körs på DC01, måste du använda den **/S** parametern (för ”system”) och ange namnet DC01 utan omvända snedstreck så här: **tasklist /S DC01**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-118">To list tasks running on DC01, you need to use the **/S** (for "system") parameter and supply the name DC01 without backslashes, like this: **tasklist /S DC01**.</span></span>

<span data-ttu-id="f2c03-119">Även om det finns viktiga tekniska skillnader mellan en tjänst och en process, är de båda exempel hanterbara elementen på en dator som har en väldefinierad livscykel.</span><span class="sxs-lookup"><span data-stu-id="f2c03-119">Although there are important technical distinctions between a service and a process, they are both examples of manageable elements on a computer that have a well-defined life cycle.</span></span> <span data-ttu-id="f2c03-120">Du kanske vill starta eller stoppa en tjänst eller en process eller hämta en lista över alla tjänster eller processer som körs.</span><span class="sxs-lookup"><span data-stu-id="f2c03-120">You may want to start or stop a service or process, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="f2c03-121">Med andra ord men en tjänst och en process är olika saker, är de åtgärder som vi utföra på en tjänst eller en process ofta begreppsmässigt samma.</span><span class="sxs-lookup"><span data-stu-id="f2c03-121">In other words, although a service and a process are different things, the actions we perform on a service or a process are often conceptually the same.</span></span> <span data-ttu-id="f2c03-122">Dessutom kanske val vi kan göra att anpassa en åtgärd genom att ange parametrar begreppsmässigt samt.</span><span class="sxs-lookup"><span data-stu-id="f2c03-122">Furthermore, choices we may make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="f2c03-123">Windows PowerShell utnyttjar dessa likheter för att minska antalet distinkta namn som du behöver veta för att förstå och använda cmdlets.</span><span class="sxs-lookup"><span data-stu-id="f2c03-123">Windows PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

### <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="f2c03-124">Använd cmdlet: ar Verb-substantiv namn att minska kommandot memorera</span><span class="sxs-lookup"><span data-stu-id="f2c03-124">Cmdlets Use Verb-Noun Names to Reduce Command Memorization</span></span>
<span data-ttu-id="f2c03-125">Windows PowerShell använder ett ”verb-substantiv” naming system där varje cmdlet-namn består av ett verb som standard med bindestreck med en specifik substantiv.</span><span class="sxs-lookup"><span data-stu-id="f2c03-125">Windows PowerShell uses a "verb-noun" naming system, where each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="f2c03-126">Windows PowerShell-verb är inte alltid engelska verb, men de express specifika åtgärder i Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="f2c03-126">Windows PowerShell verbs are not always English verbs, but they express specific actions in Windows PowerShell.</span></span> <span data-ttu-id="f2c03-127">Substantiv är mycket lik substantiv på alla språk, de beskriver specifika typer av objekt som är viktiga i systemadministration.</span><span class="sxs-lookup"><span data-stu-id="f2c03-127">Nouns are very much like nouns in any language, they describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="f2c03-128">Det är enkelt att demonstrera hur dessa två delnamn minska learning arbete genom att titta på några exempel på verb och substantiv.</span><span class="sxs-lookup"><span data-stu-id="f2c03-128">It is easy to demonstrate how these two-part names reduce learning effort by looking at a few examples of verbs and nouns.</span></span>

<span data-ttu-id="f2c03-129">Substantiv begränsas mindre, men de bör alltid beskriver ett kommando agerar på.</span><span class="sxs-lookup"><span data-stu-id="f2c03-129">Nouns are less restricted, but they should always describe what a command acts upon.</span></span> <span data-ttu-id="f2c03-130">Windows PowerShell har kommandon som **Get-Process**, **stoppa processen**, **Get-Service**, och **Stop-Service**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-130">Windows PowerShell has commands such as **Get-Process**, **Stop-Process**, **Get-Service**, and **Stop-Service**.</span></span>

<span data-ttu-id="f2c03-131">När det gäller två substantiv och två verb förenkla konsekvenskontroll inte learning mycket.</span><span class="sxs-lookup"><span data-stu-id="f2c03-131">In the case of two nouns and two verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="f2c03-132">Om du tittar på en standarduppsättning 10 verb och substantiv 10 sedan har du bara 20 ord att förstå, men dessa ord som kan användas för att bilda 100 distinkta kommandonamn.</span><span class="sxs-lookup"><span data-stu-id="f2c03-132">However, if you look at a standard set of 10 verbs and 10 nouns, you then have only 20 words to understand, but those words can be used to form 100 distinct command names.</span></span>

<span data-ttu-id="f2c03-133">Ofta kan du kan identifiera ett kommando har genom att läsa dess namn och är vanligtvis tydligt vilket namn som ska användas för ett nytt kommando.</span><span class="sxs-lookup"><span data-stu-id="f2c03-133">Frequently, you can recognize what a command does by reading its name, and it is usually apparent what name should be used for a new command.</span></span> <span data-ttu-id="f2c03-134">Till exempel en dator avstängningskommandot kanske **Stop-Computer**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-134">For example, a computer shutdown command might be **Stop-Computer**.</span></span> <span data-ttu-id="f2c03-135">Ett kommando som visar alla datorer i ett nätverk kan vara **Get-dator**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-135">A command that lists all computers on a network might be **Get-Computer**.</span></span> <span data-ttu-id="f2c03-136">Kommandot som hämtar systemdatumet är **Get-Date**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-136">The command that gets the system date is **Get-Date**.</span></span>

<span data-ttu-id="f2c03-137">Du kan visa en lista över alla kommandon som innehåller ett visst verb med den **-Verb** parameter för **Get-Command** (diskuteras **Get-Command** i detalj i nästa avsnitt).</span><span class="sxs-lookup"><span data-stu-id="f2c03-137">You can list all commands that include a particular verb with the **-Verb** parameter for **Get-Command** (We will discuss **Get-Command** in detail in the next section).</span></span> <span data-ttu-id="f2c03-138">Till exempel för att se alla cmdletar som använder verbet **hämta**, typ:</span><span class="sxs-lookup"><span data-stu-id="f2c03-138">For example, to see all cmdlets that use the verb **Get**, type:</span></span>

```
PS> Get-Command -Verb Get
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="f2c03-139">Den **-substantiv** parameter är ännu mer användbar eftersom du kan se en familj av kommandon som påverkar samma typ av objekt.</span><span class="sxs-lookup"><span data-stu-id="f2c03-139">The **-Noun** parameter is even more useful because it allows you to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="f2c03-140">Till exempel om du vill se vilka kommandon som finns för att hantera tjänster, Skriv följande kommando:</span><span class="sxs-lookup"><span data-stu-id="f2c03-140">For example, if you want to see which commands are available for managing services, type following command:</span></span>

```
PS> Get-Command -Noun Service
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

<span data-ttu-id="f2c03-141">Kommandot är inte nödvändigtvis en cmdlet bara eftersom den har ett verb-substantiv namngivningsschema.</span><span class="sxs-lookup"><span data-stu-id="f2c03-141">A command is not necessarily a cmdlet, just because it has a verb-noun naming scheme.</span></span> <span data-ttu-id="f2c03-142">Ett exempel på en intern Windows PowerShell-kommando som inte är en cmdlet men har ett verb-substantiv-namn är kommandot för att rensa konsolfönstret rensa värden.</span><span class="sxs-lookup"><span data-stu-id="f2c03-142">One example of a native Windows PowerShell command that is not a cmdlet but has a verb-noun name, is the command for clearing a console window, Clear-Host.</span></span> <span data-ttu-id="f2c03-143">Kommandot Rensa värden är faktiskt en intern funktion som du kan se om du kör Get-Command mot den:</span><span class="sxs-lookup"><span data-stu-id="f2c03-143">The Clear-Host command is actually an internal function, as you can see if you run Get-Command against it:</span></span>

```
PS> Get-Command -Name Clear-Host

CommandType     Name                            Definition
-----------     ----                            ----------
Function        Clear-Host                      $spaceType = [System.Managem...
```

### <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="f2c03-144">Använd cmdlet: ar standardparametrar</span><span class="sxs-lookup"><span data-stu-id="f2c03-144">Cmdlets Use Standard Parameters</span></span>
<span data-ttu-id="f2c03-145">Som tidigare nämnts kommandon som används i traditionella kommandoradsverktyget gränssnitt vanligtvis inte konsekventa parameternamn.</span><span class="sxs-lookup"><span data-stu-id="f2c03-145">As noted earlier, commands used in traditional command-line interfaces do not generally have consistent parameter names.</span></span> <span data-ttu-id="f2c03-146">Ibland har parametrar inte namnen på alla.</span><span class="sxs-lookup"><span data-stu-id="f2c03-146">Sometimes parameters do not have names at all.</span></span> <span data-ttu-id="f2c03-147">När de gör det gäller ofta tecken eller förkortas ord som kan skrivas snabbt men inte enkelt tolkas av nya användare.</span><span class="sxs-lookup"><span data-stu-id="f2c03-147">When they do, they are often single-character or abbreviated words that can be typed rapidly but are not easily understood by new users.</span></span>

<span data-ttu-id="f2c03-148">Windows PowerShell bearbetar parametrar direkt till skillnad från de flesta andra traditionella kommandoradsverktyget gränssnitt, och den använder den här direkt åtkomst till parametrarna tillsammans med guide för utvecklare för att standardisera parameternamn.</span><span class="sxs-lookup"><span data-stu-id="f2c03-148">Unlike most other traditional command-line interfaces, Windows PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="f2c03-149">Även om det inte garanterar att varje cmdlet kommer alltid överensstämmer med standarden, det uppmuntra den.</span><span class="sxs-lookup"><span data-stu-id="f2c03-149">Although this does not guarantee that every cmdlet will always conform to the standards, it does encourage it.</span></span>

> [!NOTE]
> <span data-ttu-id="f2c03-150">Parameternamn alltid ha en '-' till dem före när du använder dem, så att Windows PowerShell att identifiera dem som parametrar.</span><span class="sxs-lookup"><span data-stu-id="f2c03-150">Parameter names always have a '-' prepended to them when you use them, to allow Windows PowerShell to clearly identify them as parameters.</span></span> <span data-ttu-id="f2c03-151">I den **Get-Command - namn, rensa värd** exempelvis parameternamnet är **namn**, men anges den som -**namn**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-151">In the **Get-Command -Name Clear-Host** example, the parameter's name is **Name**, but it is entered as -**Name**.</span></span>

<span data-ttu-id="f2c03-152">Här följer några av de allmänna egenskaperna för standard parameternamn och användningsområden.</span><span class="sxs-lookup"><span data-stu-id="f2c03-152">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

#### <a name="the-help-parameter-"></a><span data-ttu-id="f2c03-153">Parametern hjälp (?)</span><span class="sxs-lookup"><span data-stu-id="f2c03-153">The Help Parameter (?)</span></span>
<span data-ttu-id="f2c03-154">När du anger den **-?**</span><span class="sxs-lookup"><span data-stu-id="f2c03-154">When you specify the **-?**</span></span> <span data-ttu-id="f2c03-155">Parametern till alla cmdlet cmdleten körs inte.</span><span class="sxs-lookup"><span data-stu-id="f2c03-155">parameter to any cmdlet, the cmdlet is not executed.</span></span> <span data-ttu-id="f2c03-156">I stället visar Windows PowerShell-hjälpen för cmdleten.</span><span class="sxs-lookup"><span data-stu-id="f2c03-156">Instead, Windows PowerShell displays help for the cmdlet.</span></span>

#### <a name="common-parameters"></a><span data-ttu-id="f2c03-157">Gemensamma parametrar</span><span class="sxs-lookup"><span data-stu-id="f2c03-157">Common Parameters</span></span>
<span data-ttu-id="f2c03-158">Windows PowerShell har flera parametrar som kallas *gemensamma parametrar*.</span><span class="sxs-lookup"><span data-stu-id="f2c03-158">Windows PowerShell has several parameters known as *common parameters*.</span></span> <span data-ttu-id="f2c03-159">Eftersom dessa parametrar styrs av Windows PowerShell-motorn när de implementeras med en cmdlet, fungerar de alltid på samma sätt.</span><span class="sxs-lookup"><span data-stu-id="f2c03-159">Because these parameters are controlled by the Windows PowerShell engine, whenever they are implemented by a cmdlet, they will always behave the same way.</span></span> <span data-ttu-id="f2c03-160">De gemensamma parametrarna är **WhatIf**, **Bekräfta**, **utförlig**, **felsöka**, **Varna**, **ErrorAction**, **ErrorVariable**, **OutVariable**, och **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-160">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

#### <a name="suggested-parameters"></a><span data-ttu-id="f2c03-161">Föreslagna parametrar</span><span class="sxs-lookup"><span data-stu-id="f2c03-161">Suggested Parameters</span></span>
<span data-ttu-id="f2c03-162">Windows PowerShell core-cmdletarna använder standard namn liknande parametrar.</span><span class="sxs-lookup"><span data-stu-id="f2c03-162">The Windows PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="f2c03-163">Användning av parameternamn inte tillämpas, finns det uttrycklig vägledning för användning att uppmuntra standardisering.</span><span class="sxs-lookup"><span data-stu-id="f2c03-163">Although the use of parameter names is not enforced, there is explicit guidance for usage to encourage standardization.</span></span>

<span data-ttu-id="f2c03-164">Till exempel riktlinjerna rekommenderar namn en parameter som refererar till en dator med namnet som **ComputerName**, i stället för Server, värd, System, nod eller andra vanliga alternativa ord.</span><span class="sxs-lookup"><span data-stu-id="f2c03-164">For example, the guidance recommends naming a parameter that refers to a computer by name as **ComputerName**, rather than Server, Host, System, Node, or other common alternative words.</span></span> <span data-ttu-id="f2c03-165">Bland parametern viktiga föreslagna namn är **kraft**, **undanta**, **inkludera**, **PassThru**, **sökvägen**, och **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="f2c03-165">Among the important suggested parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>